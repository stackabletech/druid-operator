apiVersion: druid.stackable.tech/v1alpha1
kind: DruidCluster
metadata:
  name: simple
spec:
  version: 0.22.0
  brokers:
    roleGroups:
      default:
        selector:
          matchLabels:
            kubernetes.io/os: stackable-linux
        replicas: 1
    config:
      javaHome: test
  coordinators:
    roleGroups:
      default:
        selector:
          matchLabels:
            kubernetes.io/os: stackable-linux
        replicas: 1
    config:
      javaHome: test
  historicals:
    roleGroups:
      default:
        selector:
          matchLabels:
            kubernetes.io/os: stackable-linux
        replicas: 1
    config:
      javaHome: test
  middlemanagers:
    roleGroups:
      default:
        selector:
          matchLabels:
            kubernetes.io/os: stackable-linux
        replicas: 1
    config:
      javaHome: test
  routers:
    roleGroups:
      default:
        selector:
          matchLabels:
            kubernetes.io/os: stackable-linux
        replicas: 1
    config:
      javaHome: test